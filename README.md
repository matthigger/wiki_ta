# wiki_ta

TA instructions: <https://matthigger.github.io/wiki_ta/>

Pages are Markdown files in `docs/`; add new ones to `nav:` in `mkdocs.yml`.
The site rebuilds on each push to `main`.

Preview locally:

```bash
pip install -r requirements.txt
mkdocs serve
```
